#include <iostream>
using namespace std;

int size = 10;
class stack22
{
    int main()
    {

        // queue implemntion
        // stack implemntion

        stack s1;
        s1.push(3);
        s1.push(4);
        s1.top_value();
        s1.pop(3);

        int arr[size];
        int top = -1;
        int push(int x);
        if (top == size)
        {
            return -1

                else(++top)
        }

        int pop()
        {
            if (top == -1)
                return -1;
            else
                top--;
        }
        delete arr[top] int top value() if (top == -1) return -1
        {
            else return arr[top]
        }
    }
}
